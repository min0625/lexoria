# Lexoria — 專案導覽

架構、指令、模組職責、關卡 pipeline、測試策略、限制條款：**一律看 `CLAUDE.md`**（單一維護點，勿在此複製）。規格細節看 `docs/word-puzzle-game-design.md` + `docs/word-puzzle-ui-screens.md`，程式註解引用其章節（如 §10），改動前先查文件。

此檔只記 CLAUDE.md/文件沒有的狀態：

- Phase 1 已完成：GitHub Pages 已部署（main 分支根目錄，legacy build），網址 https://lexoria.min0625.com/（舊網址 min0625.com/lexoria 會 301 轉址）；§17 驗收清單**全數真機測過並打勾**（2026-07-22 補完玩家編號／舊存檔補號／專屬兌換碼那 3 項）——Phase 1 驗收完成。唯一未打勾的是 2026-07-25 新加的「查詞提示」那幾條（試玩回饋的補丁，見 UI 文件 §4-F 第二段），待真機驗。
- 音效已整層移除（PR #72），唯一的聲音是答對時的 TTS 發音。兩種實作都敗給 iOS 音訊 session 行為，完整量測與 5 條死路留在設計文件 §13——**要加音效前先讀那節**，不要重走。
- 開場閘門（#gate）現在唯一的職責是喚醒 TTS 引擎（只有 click 叫得醒），拆掉會讓純拖曳的玩家整場沒發音。
- 匯出/匯入存檔：2026-07-22 拍板**暫緩至 Phase 2**（Phase 1 純單機，改 localStorage 本來就行；三筆要認的帳見設計文件 §16）。別再當待辦。
- Phase 1.5 PWA 已實作（2026-07-22，設計文件 §15 Phase 1.5）：§16 原本的「❌ 不做 PWA」已翻案，理由與快取策略在 §2／§16。**Web 版的離線可玩到這一步才第一次真的成立**（在此之前斷網是白屏）。§17 的 Phase 1.5 那 6 條也已在正式站真機驗完並打勾（2026-07-25），已找人試玩過。**還沒打勾的只剩三批**：上一條提到的「查詞提示」那幾條（2026-07-25 新加）、分享那 3 條（2026-07-27 新加，見下一條），以及 UI 補丁那批（2026-07-28 新加，見下一條），除此之外 Phase 1 + 1.5 全綠。
- 分享（2026-07-27）：修掉「電腦版分享只剩網址」，同批移除 `?code=` 兌換連結。機制與取捨在 UI 文件 §4-D（Windows 分支、提示規則）與設計文件 §9（為什麼分享不能送 `location.href`），別在這裡再抄一份。§17 那 3 條分享驗收待真機（Windows 桌機 + iOS／macOS 預覽卡片）。
- UI 補丁四項（2026-07-28）：金幣進帳時頂列數字彈一下、`.screen` 收成 480px 一欄（桌機／平板才生效）、金幣不足時提示鈕的燈泡與價格一起轉暗、重玩舊關時開場閃一次 `replayNote`。細節在 UI 文件 §2／§3（含「為什麼重玩不在頂列常駐標記」），§17 新增的 5 條待真機／VoiceOver 驗。文案本身以 UI 文件 §6 那張表為準，不在這裡抄第二份（2026-07-29 補上「重複」二字時就漏改過這裡一次）。
- App 內的 PWA 安裝引導：2026-07-27 做過一版（Chrome 系 `beforeinstallprompt` 按鈕 + iOS 文字步驟，放在設定卡）**又整個拿掉**，程式碼已還原、別再當待辦。理由與重做前提在設計文件 §15 Phase 1.5-4（§16 有一句話版）——一句話：壓掉瀏覽器自己的招攬去換一顆沒人打得開的按鈕，而安裝率沒有分析可量。
- Phase 2（Capacitor）**因成本暫緩、不是取消**：需要一台 Mac + Apple $99/年 + Google Play $25，且純 WebView 有 Apple 4.2 被拒風險。等有留存數據或要收錢再啟動；存檔搬遷的做法寫在設計文件 §15 Phase 2。好友邀請碼也押在這一步（要 `Device.getId()` 才成立，理由見設計文件 §16，別在網頁版重試）。
- `bun run fetch-data` 只需跑一次（輸出在 tools/data/，已 gitignore）；之後只需 `bun run gen`。
